# Kryvexis OS Phase 1 Backend

Lightweight API starter for the frontend shell.

## Run

```bash
npm install
npm run dev
```

The frontend can target this API with:

```bash
VITE_API_URL=http://localhost:4010
```
